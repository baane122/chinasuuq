# npm: root install only builds the web workspace + shared package.
# apps/mobile is self-contained (own node_modules + own .npmrc).
# (pnpm-era public-hoist-pattern lines removed - npm warns on them.)
