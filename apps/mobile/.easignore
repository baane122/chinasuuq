# EAS build upload ignore — this drastically shrinks the upload archive.
# node_modules is NOT uploaded; EAS installs dependencies on its own servers.
node_modules
**/node_modules

# Local / build artifacts
.git
.gitignore
.expo
.expo-shared
.DS_Store
*.log
*.tsbuildinfo
android/build
android/app/build

# Build inputs that must remain available
# Keep package-lock.json for reproducible EAS installs.

# Vercel / web-specific (irrelevant to the mobile build)
.vercel
